[
    {
      "command": "Math.floor()",
      "description": "与えられた数値以下の最大の整数を返します。console.log(Math.floor(5.95)); // Expected output: 5"
    },
    {
      "command": "parseInt(string, radix)",
      "description": "1番目の文字列を整数に変換します。2番目の引数で基数を指定できます ※10進数の場合は10"
    },
    {
      "command": "slice(start, end)",
      "description": "配列から引数のインデックス（開始～終了）の部分配列を取得できる、終了のインデックスを省略すると最後まで"
    },
    {
      "command": "[...arr1, ...arr2, ...arr3]",
      "description": "スプレッド演算子。配列と配列を結合（配列の中身バラバラになる→配列の中に入れる）"
    },
    {
      "command": "arr.map(x => x * 2)",
      "description": "map(関数) 配列の各要素に処理を適用して新しい配列を作る、元の配列は変更されない"
    },
    {
      "command": "arr.filter(x => x > 2)",
      "description": "filter(条件関数) 条件に合う要素だけを抜き出して新しい配列を作る、元の配列は変更されない"
    },
    {
      "command": "arr.includes(2)",
      "description": "includes(要素) 指定した要素が配列に含まれているかチェック"
    },
    {
      "command": "arr.join('')",
      "description": "join 配列を文字列に変換"
    },
    {
      "command": "\"a-b-c\".split('-')",
      "description": "split文字列を1文字ずつに分割して配列に（PHP の explode と同じ）数値に変換したいならconst numArr = str.split(\" \").map(Number);"
    },
    {
      "command": "arr.push(4)",
      "description": "push 配列の後ろに要素を"
    },
    {
      "command": "numbers.filter(number => number % 2 === 0)",
      "description": "配列の要素をフィルタリングして新しい配列を作成 構文 array.filter(callbackFunction)"
    },
    {
      "command": "/^\\d+$/",
      "description": "JavaScript では、スラッシュ /.../ で囲んだものが「正規表現そのもの」を表す ^ 行頭、$ 行末、\\d 数字、+直前のパターンが1回以上"
    },
    {
      "command": "numbers.reduce((total, current) => total + current, 0)",
      "description": "配列の要素の合計を求める方法"
    }
]
